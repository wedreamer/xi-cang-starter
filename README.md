# xi-cang-starter

## 环境

- nodejs 22.x (推荐使用 22.4.0 版本)

nvm 或者其他

```bash
nvm cache clear
nvm ls-remote
nvm install
corepack enable # 启用 corepack
corepack prepare pnpm@latest --activate # 使用 pnpm 作为包管理工具
```
